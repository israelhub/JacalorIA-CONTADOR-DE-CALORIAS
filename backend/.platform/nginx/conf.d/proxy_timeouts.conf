# Timeouts alinhados a analise IA (CloudFront OriginReadTimeout = 120s).
# Sem isso o nginx do EB corta ~60s e devolve HTML 504 ao app.
proxy_connect_timeout 10s;
proxy_send_timeout 120s;
proxy_read_timeout 120s;
send_timeout 120s;
client_max_body_size 10m;
